---
sidebar_class_name: hidden
description: "Returns the list of Graph scopes required to run M365Advisor."
id: Get-MtGraphScope
title: Get-MtGraphScope
hide_title: false
hide_table_of_contents: false
custom_edit_url: https://github.com/m365advisor365/m365advisor/blob/main/powershell/public/Get-MtGraphScope.ps1
---

## SYNOPSIS

Returns the list of Graph scopes required to run M365Advisor.

## SYNTAX

```powershell
Get-MtGraphScope [-SendMail] [-SendTeamsMessage] [-Privileged] [-ProgressAction <ActionPreference>]
 [<CommonParameters>]
```

## DESCRIPTION

Use this cmdlet to connect to Microsoft Graph using Connect-MgGraph.

## EXAMPLES

### EXAMPLE 1

```powershell
Connect-MgGraph -Scopes (Get-MtGraphScope)
```

Connects to Microsoft Graph with the required scopes to run M365Advisor.

### EXAMPLE 2

```powershell
Connect-MgGraph -Scopes (Get-MtGraphScope -SendMail)
```

Connects to Microsoft Graph with the required scopes to run M365Advisor and send email.

### EXAMPLE 3

```powershell
Connect-MgGraph -Scopes (Get-MtGraphScope -SendTeamsMessage)
```

Connects to Microsoft Graph with the required scopes to run M365Advisor and send messages to a Teams Channel.

### EXAMPLE 4

```powershell
Connect-MgGraph -Scopes (Get-MtGraphScope -PrivilegedScopes)
```

Connects to Microsoft Graph with the required scopes to run M365Advisor for all tests, including those requiring read write APIs.

## PARAMETERS

### -SendMail

If specified, the cmdlet will include the scope to send email (Mail.Send).

```yaml
Type: SwitchParameter
Parameter Sets: (All)
Aliases:

Required: False
Position: Named
Default value: False
Accept pipeline input: False
Accept wildcard characters: False
```

### -SendTeamsMessage

If specified, the cmdlet will include the scope to send Teams Channel Messages (ChannelMessage.Send).

```yaml
Type: SwitchParameter
Parameter Sets: (All)
Aliases:

Required: False
Position: Named
Default value: False
Accept pipeline input: False
Accept wildcard characters: False
```

### -Privileged

If specified, the cmdlet will include the scope for read write endpoints.

```yaml
Type: SwitchParameter
Parameter Sets: (All)
Aliases:

Required: False
Position: Named
Default value: False
Accept pipeline input: False
Accept wildcard characters: False
```

### -ProgressAction

\{\{ Fill ProgressAction Description \}\}

```yaml
Type: ActionPreference
Parameter Sets: (All)
Aliases: proga

Required: False
Position: Named
Default value: None
Accept pipeline input: False
Accept wildcard characters: False
```

### CommonParameters

This cmdlet supports the common parameters: -Debug, -ErrorAction, -ErrorVariable, -InformationAction, -InformationVariable, -OutVariable, -OutBuffer, -PipelineVariable, -Verbose, -WarningAction, and -WarningVariable. For more information, see [about_CommonParameters](http://go.microsoft.com/fwlink/?LinkID=113216).

## INPUTS

## OUTPUTS

## NOTES

## RELATED LINKS

[https://m365advisor.dev/docs/commands/Get-MtGraphScope](https://m365advisor.dev/docs/commands/Get-MtGraphScope)
